#!/usr/bin/python3

# replaces all occurrences of an element by another in new list.

def search_replace(my_list, search, replace):
    def s_r_elm(elm):
        return (elm if elm != search else replace)
    return list(mp(s_r_elm, my_list))
